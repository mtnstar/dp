---
- name: install dconf-cli
  apt: pkg=dconf-cli state=latest

- name: import gnome terminal settings
  become: true
  become_user: ps
  shell: 'dconf load /org/gnome/terminal/legacy/ < ~/git/psunix/profile/gnome_terminal/settings.dump'
